import { Button, Checkbox, Input } from "@Components"

function IndexPopup() {
  return (
    <div
      style={{
        display: "flex",
        flexDirection: "column",
        padding: 16
      }}>
      <Checkbox />
      <Button />
      <Input />
    </div>
  )
}

export default IndexPopup
